<section id="add-word" class="content-section">
  <h2>단어 추가하기</h2>
  <form id="addWordForm" class="add-word-form" action="/admin/word/save" method="POST">
    <div class="chapter-selection-wrapper">
      <label for="chapter">챕터</label>
      <select id="chapter" name="chapter" class="chapter-dropdown"></select>
    </div>
  
    <div class="form-group">
      <label for="word">단어</label>
      <input type="text" id="word" name="word" required>
    </div>
    <div class="form-group">
      <label for="pronunciation">읽기 방식</label>
      <input type="text" id="pronunciation" name="pronunciation" required>
    </div>
    <div id="meaningsContainer">
      <div class="meaning-group">
        <div class="form-group">
          <label for="partOfSpeech1">품사</label>
          <input type="text" id="partOfSpeech1" name="partOfSpeech1" required>
        </div>
        <div class="form-group">
          <label for="meaning1">뜻</label>
          <input type="text" id="meaning1" name="meaning1" required>
        </div>
        <div class="examples-container">
          <div class="example-group">
            <div class="form-group">
              <label for="example1_1">예문</label>
              <input type="text" id="example1_1" name="example1_1" required>
            </div>
            <div class="form-group">
              <label for="translation1_1">해석</label>
              <input type="text" id="translation1_1" name="translation1_1" required>
            </div>
          </div>
        </div>
        <button type="button" class="add-example-btn">예문 추가</button>
      </div>
    </div>
    <button type="button" id="addMeaningBtn" class="add-meaning-btn">뜻 추가</button>
    <button type="submit" id="addWordBtn" class="btn btn-primary">단어 저장</button>
  </form>
</section>

<script>
  const selectedNumber = document.getElementById('chapter');

  for (let i=1;i<=30;i++) {
    const option = document.createElement('option');
    option.value = i;
    option.textContent = i;
    selectedNumber.appendChild(option);
  }

  const zeroOption = document.createElement('option');
  zeroOption.value = 0;
  zeroOption.textContent = 0;
  selectedNumber.appendChild(zeroOption);
  selectedNumber.value = 0;
</script>

<script src="/js/addWord.js"></script>